Tree

1104. Path In Zigzag Labelled Binary Tree
      Calculate current depth of the label. Calculate offset (for each depth, values lie from 2^d -> 2^(d+1) -1.
      Find the real parent based on offset. Repeat until we reach 1
      
HashTable

138. Copy List with Random Pointer
     ceate a hashmap to store <old, new> node pairs to save time.

204. Count Primes
     if an element e is prime, e * 2, e * 3, e * 4... aren't prime.
     
